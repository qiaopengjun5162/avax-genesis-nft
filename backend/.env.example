# 签名后端私钥（独立于部署者；本地生成，勿提交 git）
# 该私钥对应地址必须是链上 signer()，否则签出来的名全被拒绝
SIGNER_PRIVATE_KEY=
PORT=8787
# 开发期 CORS 白名单（默认 *；上线改成前端域名）
CORS_ORIGIN=
# 目标合约（留空 = Fuji 演示实例 0x7AD0921D80CeFC98889a84B960c74866A452Cc2A）
CONTRACT_ADDRESS=
# 自定义 RPC（公共节点限流时换成自己的）
FUJI_RPC=
